const actionNames = { criacao: 'Criação', alteracao: 'Alteração', exclusao_logica: 'Exclusão lógica', registro_falta: 'Registro de falta', substituicao: 'Substituição', criacao_escala: 'Criação de escala', alteracao_escala: 'Alteração de escala' };

export default function AuditTable({ records }) {
  if (!records.length) return <div className="rounded-xl border bg-card py-16 text-center text-sm text-muted-foreground">Nenhum evento encontrado.</div>;
  return <div className="overflow-hidden rounded-xl border bg-card"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-muted/60 text-left text-xs uppercase text-muted-foreground"><tr><th className="p-3">Data</th><th className="p-3">Ação</th><th className="p-3">Área</th><th className="p-3">Alteração</th><th className="p-3">Responsável</th></tr></thead><tbody className="divide-y">{records.map((record) => <tr key={record.id} className="align-top"><td className="whitespace-nowrap p-3 text-muted-foreground">{new Date(record.created_date).toLocaleString('pt-BR')}</td><td className="p-3 font-medium">{actionNames[record.action] || record.action}</td><td className="p-3">{record.entity_type}</td><td className="min-w-64 p-3"><p>{record.field || record.reason || 'Registro completo'}</p>{(record.old_value || record.new_value) && <p className="mt-1 text-xs text-muted-foreground">{record.old_value || '—'} → {record.new_value || '—'}</p>}</td><td className="p-3 text-muted-foreground">{record.responsible_user || 'Sistema'}</td></tr>)}</tbody></table></div></div>;
}