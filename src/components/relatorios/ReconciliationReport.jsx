import { brl, fmt } from '@/lib/financialReportUtils';
export default function ReconciliationReport({cards,delivery,reconciliations}){
 const expected=cards.reduce((s,x)=>s+Number(x.net_amount||0),0)+delivery.reduce((s,x)=>s+Number(x.net_amount||0),0),differences=reconciliations.reduce((s,x)=>s+Number(x.difference||0),0),divergent=reconciliations.filter(x=>x.status==='divergente').length;
 return <div className="space-y-3"><div className="grid grid-cols-3 gap-3"><Stat l="Repasses previstos" v={brl(expected)}/><Stat l="Diferença apurada" v={brl(differences)}/><Stat l="Divergências" v={divergent}/></div><div className="rounded-xl border bg-card overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-sm"><thead className="bg-muted"><tr>{['Data','Referência','Esperado','Encontrado','Diferença','Conta','Status'].map(h=><th key={h} className="px-4 py-3 text-left font-medium">{h}</th>)}</tr></thead><tbody className="divide-y">{reconciliations.map(x=><tr key={x.id}><td className="px-4 py-3">{fmt(x.date)}</td><td className="px-4 py-3 font-medium">{x.reference_description||x.reference_type}</td><td className="px-4 py-3">{brl(x.expected_amount)}</td><td className="px-4 py-3">{brl(x.actual_amount)}</td><td className={`px-4 py-3 font-semibold ${Number(x.difference)<0?'text-rose-700':'text-emerald-700'}`}>{brl(x.difference)}</td><td className="px-4 py-3">{x.financial_account_name||'—'}</td><td className="px-4 py-3 capitalize">{x.status}</td></tr>)}{!reconciliations.length&&<tr><td colSpan="7" className="p-10 text-center text-muted-foreground">Sem conciliações no período.</td></tr>}</tbody></table></div></div></div>;
}
function Stat({l,v}){return <div className="rounded-xl border bg-card p-4"><p className="text-xs text-muted-foreground">{l}</p><p className="mt-1 text-lg font-semibold">{v}</p></div>}